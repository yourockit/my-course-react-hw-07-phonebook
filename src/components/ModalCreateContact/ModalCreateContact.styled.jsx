import styled from '@emotion/styled';
import { Form } from 'formik';

export const Container = styled(Form)`
  display: none;
  position: absolute;
  width: 100%;
  height: 100%;
  background: #ffffff;
  margin-right: auto;
  margin-left: auto;
`;
